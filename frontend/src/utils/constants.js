// Frontend configuration constants
export const CONSTANTS = {
  // Number of recent trades to display in history
  TRADE_HISTORY_DISPLAY_LIMIT: 10,
  
  // API polling intervals (milliseconds)
  STATUS_POLL_INTERVAL: 5000,
  DATA_POLL_INTERVAL: 10000,
};
